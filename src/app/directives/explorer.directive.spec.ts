import { ExplorerDirective } from './explorer.directive';

describe('ExplorerDirective', () => {
  it('should create an instance', () => {
    const directive = new ExplorerDirective();
    expect(directive).toBeTruthy();
  });
});
